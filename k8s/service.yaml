apiVersion: v1
kind: Service
metadata:
  name: node-demo-service
  namespace: demo-app
spec:
  selector:
    app: node-demo
  type: NodePort
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080
