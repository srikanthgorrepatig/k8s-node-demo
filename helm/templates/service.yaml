apiVersion: v1
kind: Service
metadata:
name: {{ .Chart.Name }}-svc
namespace: demo-app
spec:
type: {{ .Values.service.type }}
selector:
app: {{ .Chart.Name }}
ports:
- protocol: TCP
port: {{ .Values.service.port }}
targetPort: 3000